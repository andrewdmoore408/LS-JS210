let x = '13';
let y = 9;

console.log(x * y);

// Mathematical operators other than + are only defined for numbers in JS, so the value x points to will be coerced to a number and the numeric product of 13 and 9 will be returned.
